# ------------------------------------------------------------
# Help global — liste tous les helpers disponibles
# ------------------------------------------------------------

help() {
  echo ""
  echo "  📖  Aliases Cheatsheet — Vue d'ensemble"
  echo "  ══════════════════════════════════════════════════════"
  echo ""
  echo "  Tape la commande correspondante pour le détail complet"
  echo ""
  echo "  🐳  dhelp     → Docker        (compose, containers, images, stats)"
  echo "  🌿  ghelp     → Git           (add, commit, push, rebase, stash...)"
  echo "  🎵  sfhelp    → Symfony       (cache, doctrine, debug, make, tests)"
  echo "  🧭  navhelp   → Navigation    (listing, tree, recherche, filesystem)"
  echo "  ⚙️   syshelp   → System        (réseau, processus, archives, macOS)"
  echo "  🤖  aihelp    → AI / Ollama   (modèles, runtime, logs, navigation)"
  echo ""
  echo "  ══════════════════════════════════════════════════════"
  echo ""
}
